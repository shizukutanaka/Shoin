# Shoin プロダクトレビュー — 長所・短所・改善案 (v0.2.165 時点、2026-09-02 更新)

shoin/ 15モジュール + 単一HTML UI・テスト710件・ドキュメント一式の精査結果。
初版は 2026-06-13 (v0.1.0)、全面更新は 2026-07-18 (v0.2.133)、以後は変更のあった項目のみ追記。
初版の詳細な指摘→修正の往復記録は CLAUDE.md の Version History(v0.1.37〜)と
CHANGELOG.md(〜v0.1.55)にバグ単位で残っているため、ここでは現時点の評価に集中する。

## 長所 (維持すべき差別化資産)

1. **引用の機械検証(四段)+ 出所の全面表示** — 競合に無い核。範囲外番号検出・
   字句グラウンディング確認・誤帰属検出・無出典断定文検出(`citation.py`)。
   v0.2.130-132 で節文脈(どの見出しの下から引用したか)を Web シールビューア・
   Markdownエクスポート・CLI レポートの3面すべてに露出し、出所の透明性が一貫。
2. **研究裏付けのある検索改善** — コンテキスト付きチャンク(見出しパンくず+タイトルを
   FTS/埋め込みに併走索引, v0.2.123)と マルチクエリ RAG-Fusion(`SHOIN_MULTI_QUERY`,
   DMQR-RAG系, v0.2.125)。どちらも依存ゼロ・オフラインの設計制約内で実装。
3. **依存最小・単一プロセス** — ランタイム依存は pypdf のみ。stdlib HTTPサーバ +
   ビルド不要の単一HTML。
4. **graceful degradation が一級市民** — LLM不在→検索のみ、埋め込み不在→BM25のみ、
   マルチクエリ書き換え失敗→単一クエリ、すべてテストで担保。
5. **SSRF防御の質** — IPピン留めで DNS リバインディングまで遮断(ADR-001)。
6. **日本語一次設計** — FTS5 trigram + CJK対応トークン推定 + かな相互変換 +
   cp932/UTF-16 フォールバックデコード。全角引用括弧 `［Ｓ１］` も UI まで対応。
7. **診断可能性** — `GET /api/health` / `shoin health` / `SHOIN_DEBUG=1` 検索統計。
   設定が「効いているか」をコードを読まずに確認できる。
8. **工学的規律** — 追記専用migration(並行冪等, v0.2.128)、安定エラーコード体系、
   fail-then-pass 必須の回帰テスト文化、UI変更の実ブラウザ(Playwright)検証文化、
   mypy --strict ゼロ。文書の主張とコードの実挙動の突合を繰り返す監査文化
   (v0.2.75/112/113/129 で「文書だけの空約束」を4件検出・解消)。スキーマの
   **ダウングレード安全性も検証済み**(v0.2.158): migration は追記専用・加算的
   (DEFAULT付き列追加/新テーブルのみ)なので、旧バイナリが新スキーマDBを開いても
   `version <= current` 全スキップでそのまま動く。
9. **XSS安全な UI とアクセシビリティ** — 描画は `textContent`/`createTextNode` のみで
   `innerHTML` 不使用(LLM出力・ソース本文の HTML/スクリプト注入が原理的に無害)。
   全 `aria-label` が i18n 経由(`data-i18n-aria`, v0.2.71)で JA/EN スクリーンリーダー対応。
10. **プライバシー主張が反証テスト済み**(v0.2.158) — 「完全ローカル」は宣伝文句ではなく
    列挙で裏付けた事実: 全コードベースの outbound 通信は `llm.py`(ユーザー設定の
    `SHOIN_LLM_URL` のみ)と `ingest.py`(ユーザーが要求した URL、SSRF ピン留め付き)の
    2ファイルだけ。UI の `fetch()` は相対 `/api/…` への1箇所のみで、外部 CDN・フォント・
    アナリティクス等の外部 `src`/`href` 資産はゼロ。ユーザーが指定した先以外へデータを
    送るコードパスは存在しない。

## 短所 (現存する正直な弱点)

| # | 弱点 | 現状 |
|---|------|------|
| 1 | **GitHub Actions CI は未稼働**(ただし自動検証は稼働) | `.github/workflows/` は不在。GitHub App の `workflows` 権限制約は v0.2.153 で実測再確認(push が remote rejected)。ただし「CIをGitHubで回す」は手段であって要件ではなく、本当の要件「landする前に自動検証される」は `scripts/verify.sh` + `.githooks/pre-push`(`git config core.hooksPath .githooks`)で権限なしに達成済み。`ci/ci.yml` 側も全ゲートを実測し赤くなる3点を修正済み(ci/README.md 参照) |
| 8 | **ブラウザ描画の自動テストは無い**(静的契約テストは v0.2.154 で導入) | レンダリング・レイアウト・イベント配線の回帰は依然ライブ検証のみ。ただし「ブラウザが必要な壊れ方」以外は `tests/test_ui_contract.py` が恒久カバー: JS構文(`node --check`、node不在時はSKIP)・data-i18n キーの ja/en 両locale完備・UIが叩く `/api/…` が実在ルートに一致するか。依存追加ゼロ、3件とも故意の破壊で捕捉を実証済み |
| 2 | ~~**`ruff format` 不整合**~~ (v0.2.153 で決着) | 第一原理で解決: プロジェクトは意図的に手整形(桁揃えコメント)しており、従わないと決めたスタイルを CI で強制するのは壊れたゲート。`ruff format --check` ステップを削除し、`ruff check` をルール集合ピン留めの上で唯一の lint 契約とした |
| 3 | ~~**CHANGELOG.md が v0.1.55 で停止**~~ (注記で決着) | 正史は CLAUDE.md の Version History 節。CHANGELOG.md 冒頭の注記がそこへ誘導し、v0.2.151 で版数非依存化済み(この行自体が台帳の更新漏れだった — v0.2.161 のバックログ監査で検出) |
| 4 | **PyPI 未発行**(インストール自体は可能・検証済) | v0.2.156 で README の**虚偽の約束を除去**: `pip install shoin` は実際に失敗する(`No matching distribution found`, 実測)。現行の案内は clone + `pip install .` で、clone→install→`shoin serve`→HTTP 200 まで実機検証済。PyPI 公開は利便性であってインストール可否の要件ではない。残作業は `twine upload` の認証情報のみ(エージェント不可) |
| 5 | ~~**改名後の埋め込み陳腐化**~~ (v0.2.160 で解消) | `pipeline.rename_source()` が改名時に当該ソースのチャンクのみ再埋め込みする。実測していた欠陥(改名後、新タイトルで引くと cosine 0.0・無関係文書の下位)は解消。埋め込み無効/到達不能/モデル不一致では従来どおり無害に degrade |
| 9 | **大規模ノートブックの検索レイテンシ**(実測・v0.2.162-164 で約4倍改善) | ベクトル検索は全チャンクを Python で走査するため件数に線形。実測 20,000チャンクで **376ms**(改善前 1,489ms、累計3.96倍)、文書化上限 50,000 では約0.94秒/クエリ(改善前 約3.7秒)。BM25 側は SQLite が C で処理するため平坦(20,000件で45ms)。メモリも解決済み(v0.2.163: 1クエリの確保量が件数に非依存の O(k) へ、上限で約184MB → ほぼゼロ)。内訳はクエリ側ノルムのホイスト・list生成の除去(v0.2.162)、上位k件のみ実体化(v0.2.163)、チャンク側ノルムのキャッシュ(v0.2.164, migration 7)。残るのはドット積そのもので、依存ゼロ制約の下ではこれ以上は削れない |
| 10 | ~~**大きな文書の取込が遅い**~~ (v0.2.165 で解消) | 実測: 分割の98%が `estimate_tokens`、その中で `is_cjk()` が1MBあたり387万回・範囲表の線形走査。正規表現クラス(C側で1パス)+ 境界の二分探索へ置換し、分割は 1MBで2.77s→0.12s、5MBで13.62s→0.59s(23倍)。文書化上限直下の9.4MBで `index_source` 全体が約25秒→3.03秒 |
| 6 | **トークン予算の制約** | 2400トークン総予算・TOP_K=8。長文ソースの切詰め、10ターン超会話の履歴脱落。軽量LLM前提の意図的設計だが限界でもある |
| 7 | ~~**デフォルトブランチが古い版を指す**~~ (v0.2.159 で実害解消) | 実害だった「ref 省略 `pip install git+<repo>` が v0.2.141 を掴む」は解消: PR #6 で作業ブランチを main へマージし、PR #7 でデフォルトブランチの**内容**を main と同一に同期した。残るのは設定上のブランチ名切替(Settings → General → Default branch → `main`)のみで、これは管理者の1クリック・現在は無害な見た目の問題 |

## 改善案バックログ (優先度順・作業指示書から参照される台帳)

各項目に担当推奨(→ `docs/agents/opus.md` / `docs/agents/sonnet.md` 参照)を付す。
旧 #10「監査ラウンドの継続」は成果物ではなく作業習慣なので台帳から削除(v0.2.161)— 手順は `docs/agents/opus.md` §5 にある。

| # | 改善案 | 難易度 | 担当推奨 | エントリポイント / 備考 |
|---|--------|--------|----------|--------------------------|
| 1 | **GitHub Actions への CI 移設**(自動検証自体は稼働済) | 人手のみ | リポジトリ管理者 | `git mv ci/ci.yml .github/workflows/ci.yml`。エージェントには権限がない(v0.2.153 で実測再確認)。**急ぎではない**: 本当の要件「landする前の自動検証」は `git config core.hooksPath .githooks` で既に達成済み。移設時は ci.yml が実測済みで全ゲート緑になることも確認済み |
| 2 | ~~**`ruff format` 方針決定**~~ | — | 完了 (v0.2.153) | ステップ削除で決着。短所#2 参照 |
| 3 | **PyPI 発行**(検証は完了) | 人手のみ(認証) | リポジトリ管理者 | ビルド・install・static同梱は v0.2.151 で検証済(短所#4参照)。残作業は `twine upload` の認証情報のみ、エージェント不可 |
| 4 | ~~**CHANGELOG.md の追補 or アーカイブ宣言**~~ | — | 完了 (v0.2.151) | 冒頭注記で「正史は CLAUDE.md」と明示・版数非依存化済み。台帳側の閉鎖漏れを v0.2.161 で修正 |
| 5 | **デフォルトブランチ名の切替**(実害は v0.2.159 で解消済) | 人手のみ・1クリック | リポジトリ管理者 | GitHub Settings → General → Default branch → `main`。内容は既に main と同一なので急ぎではない |
| 6 | **リリースタグ/Release 作成** | 人手のみ | リポジトリ管理者 | エージェントのタグ push はプロキシが 403 で拒否(検証済み) |
| 7 | ~~**改名時の埋め込み再計算(選択的)**~~ | — | 完了 (v0.2.160) | `pipeline.rename_source()` として実装。論点だった2件は解決: LLM 到達不能は `_embed_chunks` が握り潰し改名は必ず成功、モデル不一致は `force=False` でガードを尊重 |
| 8 | ~~**serve 起動ログの i18n**~~ | — | 完了 (v0.2.161) | `server.py` に最小の `_STRINGS`/`_t`(export.py と同型)。`SHOIN_LANG=en` で英語起動ログ。文字列表を直接テスト |
| 9 | ~~**Studio 出力への節文脈表示の拡充**~~ | — | 完了 (v0.2.161) | Markdown エクスポートの Studio カードにも `引用元: S1=題 (§ 節)` 凡例を出す。凡例構築を `_legend()` に共通化しチャット側と乖離不能に |
| 11 | **Playwright スモークテストを CI へ** | — | **見送り(判断済み, v0.2.161)** | 安価な8割は静的契約テスト(`tests/test_ui_contract.py`)で恒久カバー済み。残る描画/イベント配線の2割のために Playwright + ブラウザ DL という重依存を足すのは「依存ゼロ」原則に見合わない。UI 変更時のライブ検証(既定の運用)で担保する。再検討条件: UI が複数ファイル/ビルド工程を持つようになった時 |

## 結論

v0.1.0 時点の結論「設計判断(軽量・検証・日本語の3点集中)は正しく、出荷水準」は
維持どころか強化された。v0.2 系では検索品質(コンテキスト索引・RAG-Fusion)と
出所透明性(節文脈の3面表示)という差別化の本丸を、依存ゼロ制約を守ったまま
研究裏付き実装で伸ばし、同時に50回超の監査ラウンドで並行性・エンコーディング・
トークン推定などの深部バグを fail-then-pass 規律で潰し続けた。残る弱点は
コード品質ではなく**配布面のみ**である。v0.2.150-156 でエンジニアリング側は
出し切った: 死コード227行削除、ホットパス4.1倍高速化、権限の壁を迂回した自動検証
(`scripts/verify.sh` + pre-push フック)、UI契約テスト、そして README の虚偽の
インストール手順の修正(`pip install shoin` は実際に失敗する — 実測)。
実機検証も完了: clone → `pip install .` → `shoin serve` → 生成 → 引用検証(✓根拠確認済み)
→ エクスポート → SSE ストリーミング → SSRF/405/403 の防御まで通しで動作確認済み。
作業ブランチは main を取り込み済みで **fast-forward マージ可能**(コンフリクト risk ゼロ)。
配送操作も v0.2.159 で実行可能な限り完了した: **main へのマージは実施済み**(PR #6)、
**デフォルトブランチの内容も main と同一に同期済み**(PR #7 — ref 省略インストールが
最新版を掴むことを以て実害解消)。残るのは認証情報・管理画面が要る3点のみ
(ブランチ名設定の切替=無害な1クリック、PyPI 公開、リリースタグ)で、
いずれも欠けている実装ではない。
v0.2.158 のソクラテス監査では本レビューの長所側の主張そのものを反証にかけ、
中核主張(完全ローカル・依存最小・XSS安全・SSRF防御・スキーマ互換)を全件実証 —
反証は一件も出なかった。
